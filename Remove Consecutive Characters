int n = S.size();
    std::string ans = ""; 
    for (int i = 0; i < n; i++) {
        int n1 = ans.size(); 
        if (n1 == 0 || S[i] != ans[n1 - 1]) {
            ans += S[i];
        }
    }
    return ans;
